- เข้าใจว่า event โต้ตอบได้ เช่น click, input, keypress
- ได้ฝึกให้เว็บตอบสนองตามพฤติกรรมของผู้ใช้ 
- รู้ว่าทำไมต้องแยก logic ของแต่ละ event handler ออกมาให้ชัดเจน
- เริ่มมองเห็น flow ของ user interaction เช่น เมื่อผู้ใช้กรอก → ตรวจ → แสดงผล
- ได้ฝึกแก้ปัญหาบั๊คพวก event ซ้อนกัน