- ได้เข้าใจว่าเว็บทำงานกับข้อมูลผ่านการดึงข้อมูลจาก API
- เรียนรู้การใช้ fetch() ทั้งแบบ promise และ async/await
- ได้ฝึกประมวลผล JSON แสดงผลบนหน้าเว็บและจัดการ loading error
- เห็นภาพชัดเจนขึ้นว่า frontend เชื่อม backend อย่างไง
- เริ่มเข้าใจกระบวนการของแอปว่า ดึงข้อมูล → แสดงผล → อัปเดต UI